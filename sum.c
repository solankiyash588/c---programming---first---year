#include<stdio.h>
int main()
{
    int a,b, sum ;
    printf("enter value of a :");
    scanf("%d",&a);
    printf("enter value of b :");
    scanf("%d",&b);
    sum = (a+b);
    printf("sum of two numbers is %d ",sum);
    return 0;
}